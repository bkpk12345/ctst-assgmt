const { Comment } = require("./comment.model");
const { Post } = require("./post.model");
const { Todo } = require("./todo.model");
const { User } = require("./user.model");

module.exports = { Comment, Post, Todo, User };
